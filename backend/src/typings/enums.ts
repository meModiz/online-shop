export enum Role {
  USER = "USER",
  ADMIN = "ADMIN",
}

export enum Category {
  PHONES = "phones",
  COMPUTERS = "computers",
  SMARTWATCHES = "watches",
  CAMERAS = "cameras",
  HEADPHONES = "audio",
  GAMING = "gaming",
}
